<h1 class="page-title">
    <i class="fa-solid fa-arrow-down"></i>
    Baixa de Correias — <%= equipamento.nome %>
</h1>

<div class="card" style="padding:25px;">


    <!-- BOTÕES PRINCIPAIS DO EQUIPAMENTO -->
    <div style="display:flex; gap:14px; flex-wrap:wrap; margin-bottom:25px;">

        <a href="/equipamentos/<%= equipamento.id %>"
           class="btn-history btn-stack"
           style="background:#6c757d; width:95px; height:95px;">
            <i class="fa-solid fa-arrow-left"></i>
            <span class="btn-label">Voltar</span>
        </a>

        <a href="/equipamentos/<%= equipamento.id %>/editar"
           class="btn-edit btn-stack"
           style="width:95px; height:95px;">
            <i class="fa-solid fa-pen"></i>
            <span class="btn-label">Editar</span>
        </a>

        <a href="/equipamentos/<%= equipamento.id %>/qrcode"
           class="btn-qr btn-stack"
           style="width:95px; height:95px;">
            <i class="fa-solid fa-qrcode"></i>
            <span class="btn-label">QR Code</span>
        </a>

        <a href="/equipamentos/<%= equipamento.id %>/correias"
           class="btn-correias btn-stack"
           style="width:95px; height:95px; background:#b7e4c7;">
            <i class="fa-solid fa-gears"></i>
            <span class="btn-label" style="color:#054a29;">Correias</span>
        </a>

        <a href="/equipamentos/<%= equipamento.id %>/historico"
           class="btn-history btn-stack"
           style="width:95px; height:95px;">
            <i class="fa-solid fa-clock-rotate-left"></i>
            <span class="btn-label">Histórico</span>
        </a>

    </div>



    <!-- FORMULÁRIO DE BAIXA -->
    <form method="POST" action="/equipamentos/<%= equipamento.id %>/baixar-correia" class="form-card">

        <h3 style="margin-bottom:18px; color:#124d2b;">Registrar Baixa de Correia</h3>

        <label>Correia usada</label>
        <select name="correia_id" required>
            <option value="">Selecione uma correia</option>
            <% correias.forEach(c => { %>
                <option value="<%= c.id %>">
                    <%= c.nome %> — <%= c.modelo %> — Estoque: <%= c.quantidade %>
                </option>
            <% }) %>
        </select>

        <label>Quantidade usada</label>
        <input type="number" name="quantidade" min="1" value="1" required>

        <button class="btn-primary" sty
