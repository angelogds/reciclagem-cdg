<%
    // Variáveis usadas no layout
    const title = "Equipamentos";
    const active = "equipamentos";
%>

<h1 class="page-title">Equipamentos</h1>

<a href="/equipamentos/novo" class="btn-add">+ Novo Equipamento</a>

<table class="tabela">
    <tr>
        <th>ID</th>
        <th>Imagem</th>
        <th>Nome</th>
        <th>Código</th>
        <th>Local</th>
        <th>QR</th>
        <th>Ações</th>
    </tr>

    <% equipamentos.forEach(e => { %>
        <tr>
            <td><%= e.id %></td>

            <td>
                <% if (e.imagem) { %>
                    <img src="/<%= e.imagem %>" class="thumb">
                <% } else { %>
                    <span class="sem-img">Sem imagem</span>
                <% } %>
            </td>

            <td><%= e.nome %></td>
            <td><%= e.codigo || "-" %></td>
            <td><%= e.local || "-" %></td>

            <td>
                <a href="/equipamentos/<%= e.id %>/qrcode" class="btn-qr">
                    <i class="fa-solid fa-qrcode"></i>
                </a>
            </td>

            <td>
                <a href="/equipamentos/<%= e.id %>/editar" class="btn-edit">Editar<
