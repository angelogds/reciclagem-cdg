<%- include("./partials/cabecalho") %>

<h1 class="page-title">
    <i class="fa-solid fa-file-circle-plus"></i>
    Abrir Nova Ordem de Serviço
</h1>

<div class="card form-card">

    <% if (error) { %>
        <div class="alert-error"><%= error %></div>
    <% } %>

    <form method="POST" action="/os/nova" class="formulario">

        <div class="grupo">
            <label>Equipamento</label>
            <select name="equipamento_id" required>
                <option value="">Selecione...</option>
                <% equipamentos.forEach(eq => { %>
                    <option value="<%= eq.id %>"><%= eq.nome %></option>
                <% }) %>
            </select>
        </div>

        <div class="grupo">
            <label>Tipo</label>
            <select name="tipo" required>
                <option value="Mecânica">Mecânica</option>
                <option value="Elétrica">Elétrica</option>
                <option value="Preventiva">Preventiva</option>
                <option value="Corretiva">Corretiva</option>
            </select>
        </div>

        <div class="grupo">
            <label>Descrição</label>
            <textarea name="descricao" rows="4" required></textarea>
        </div>

        <button class="btn btn-verde-claro btn-full" style="margin-top:8px;">
            <i class="fa-solid fa-floppy-disk"></i> Abrir OS
        </button>
    </form>

</div>

<div class="voltar-box">
    <a href="/ordens" class="btn btn-cinza-escuro">
        ← Voltar
    </a>
</div>

<%- include("./partials/rodape") %>
