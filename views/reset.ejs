<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />

    <title>Redefinir Senha | Campo do Gado</title>

    <link rel="stylesheet" href="/css/style.css">

    <style>
        /* mantive estilo original premium */
        body {
            margin: 0;
            padding: 0;
            font-family: "Inter", sans-serif;
            background: url('/img/login-bg.png') no-repeat center center;
            background-size: cover;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
    </style>
</head>

<body>
<div class="reset-box">

    <h2 class="reset-title">Nova Senha</h2>

    <% if (error) { %>
        <div class="alert-error"><%= error %></div>
    <% } %>

    <form method="POST" action="/reset/<%= token %>">
        <label>Nova Senha</label>
        <input type="password" name="senha" required>
        <button class="btn-reset">Salvar</button>
    </form>

    <a href="/login" class="reset-back">Voltar ao login</a>

</div>
</body>
</html>
