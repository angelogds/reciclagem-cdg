<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">

<title>Relatório de Correias</title>

<style>
    body {
        font-family: Arial, sans-serif;
        font-size: 13px;
        margin: 20px;
    }

    h1 {
        text-align: center;
        margin-bottom: 8px;
        font-size: 22px;
        color: #0D6B33;
    }

    .sub {
        text-align: center;
        font-size: 13px;
        margin-bottom: 25px;
        color: #555;
    }

    table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 15px;
        font-size: 12px;
    }

    th {
        background: #0D6B33;
        color: white;
        padding: 8px;
        font-size: 12px;
        border: 1px solid #ccc;
    }

    td {
        padding: 7px;
        border: 1px solid #ddd;
    }

    .right {
        text-align: right;
    }

    .center {
        text-align: center;
    }

    .footer {
        margin-top: 35px;
        text-align: center;
        font-size: 11px;
        color: #666;
    }

</style>
</head>

<body>

    <!-- LOGO (se enviada do server.js) -->
    <% if (logo) { %>
        <div style="text-align:center; margin-bottom:15px;">
            <img src="<%= logo %>" style="width:85px;">
        </div>
    <% } %>

    <h1>Relatório de Correias</h1>
    <div class="sub">
        Estoque completo atualizado em <%= dataGeracao %>
    </div>

    <table>
        <thead>
            <tr>
                <th style="width:45px;">ID</th>
                <th>Nome</th>
                <th>Modelo</th>
                <th>Medida</th>
                <th style="width:70px;" class="center">Qtd</th>
            </tr>
        </thead>

        <tbody>

            <% correias.forEach(c => { %>
            <tr>
                <td class="center"><%= c.id %></td>
                <td><%= c.nome %></td>
                <td><%= c.modelo %></td>
                <td><%= c.medida %></td>
                <td class="center"><%= c.quantidade %></td>
            </tr>
            <% }) %>

            <% if (correias.length === 0) { %>
                <tr>
                    <td colspan="5" class="center" style="padding:12px;">
                        Nenhuma correia cadastrada.
                    </td>
                </tr>
            <% } %>

        </tbody>
    </table>

    <div style="margin-top:20px; font-size: 14px;">
        <strong>Total de itens diferentes:</strong> <%= correias.length %>
    </div>

    <div class="footer">
        Campo do Gado — Sistema de Manutenção  
        <br>
        Relatório gerado automaticamente.
    </div>

</body>
</html>
