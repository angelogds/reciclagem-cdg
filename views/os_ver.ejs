<h1 class="page-title">
    <i class="fa-solid fa-file-lines"></i>
    Ordem de Serviço #<%= os.id %>
</h1>

<div class="card form-card">

    <!-- STATUS -->
    <div style="margin-bottom: 12px;">
        <% if (os.status === 'aberta') { %>
            <span class="tag warn" style="font-size:14px;">🔧 Aberta</span>
        <% } else { %>
            <span class="tag green" style="font-size:14px;">✔ Finalizada</span>
        <% } %>
    </div>

    <!-- DADOS -->
    <div class="form-grid">
        <div>
            <label>Equipamento</label>
            <input type="text" value="<%= equipamento.nome %>" disabled class="input-disabled">
        </div>

        <div>
            <label>Tipo</label>
            <input type="text" value="<%= os.tipo %>" disabled class="input-disabled">
        </div>

        <div>
            <label>Abertura</label>
            <input type="text" value="<%= os.aberta_em %>" disabled class="input-disabled">
        </div>

        <% if (os.fechada_em) { %>
        <div>
            <label>Finalizada em</label>
            <input type="text" value="<%= os.fechada_em %>" disabled class="input-disabled">
        </div>
        <% } %>
    </div>

    <label>Descrição</label>
    <textarea disabled class="input-disabled" rows="4"><%= os.descricao %></textarea>
</div>

<!-- BOTÕES PRINCIPAIS -->
<div class="actions" style="margin-top:20px; flex-wrap:wrap;">

    <!-- EDITAR -->
    <a href="/os/<%= os.id %>/editar" class="btn-edit btn-stack">
        <i class="fa-solid fa-pen"></i>
        <span class="btn-label">Editar</span>
    </a>

    <!-- FINALIZAR OS -->
    <% if (os.status === 'aberta') { %>
    <form method="POST" action="/os/<%= os.id %>/finalizar">
        <button class="btn-history btn-stack">
            <i class="fa-solid fa-check"></i>
            <span class="btn-label">Finalizar</span>
        </button>
    </form>
    <% } %>

    <!-- PDF -->
    <a href="/os/<%= os.id %>/pdf" class="btn-qr btn-stack">
        <i class="fa-solid fa-file-pdf"></i>
        <span class="btn-label">PDF</span>
    </a>

    <!-- HISTÓRICO DO EQUIPAMENTO -->
    <a href="/equipamentos/<%= equipamento.id %>/historico" class="btn-history btn-stack">
        <i class="fa-solid fa-clock-rotate-left"></i>
        <span class="btn-label">Histórico</span>
    </a>

    <!-- VOLTAR -->
    <a href="/os" class="btn-correias btn-stack">
        <i class="fa-solid fa-arrow-left"></i>
        <span class="btn-label">Voltar</span>
    </a>

</div>
