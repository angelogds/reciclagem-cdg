<%- include("./partials/cabecalho") %>

<h1 class="page-title">
    <i class="fa-solid fa-qrcode"></i>
    QR Code — <%= equipamento.nome %>
</h1>

<div class="card" style="text-align:center; padding:30px;">

    <div style="margin-bottom:25px;">
        <% if (qr) { %>
            <img src="<%= qr %>" 
                 alt="QR Code" 
                 style="width:240px; height:240px; border-radius:16px; box-shadow:0 4px 18px rgba(0,0,0,0.25);">
        <% } else { %>
            <p>QR Code não disponível.</p>
        <% } %>
    </div>

    <p style="font-size:16px; color:#0d4d2c;">
        <strong>URL do equipamento:</strong><br>
        <span style="word-break:break-all;"><%= url %></span>
    </p>

    <div style="margin-top:20px;">
        <a href="/equipamentos" class="btn btn-cinza-escuro">← Voltar</a>
    </div>

</div>

<%- include("./partials/rodape") %>
