<%- include("./partials/cabecalho") %>

<h1 class="page-title">
    <i class="fa-solid fa-lock"></i>
    Fechar Ordem de Serviço
</h1>

<div class="card form-card">

    <h2 style="color:var(--green-900); margin-bottom:10px;">
        OS #<%= ordem.id %> — <%= ordem.equipamento_nome %>
    </h2>

    <p><strong>Descrição:</strong> <%= ordem.descricao %></p>

    <form method="POST" action="/ordens/<%= ordem.id %>/fechar" enctype="multipart/form-data" class="formulario">

        <div class="grupo">
            <label>Resultado / Serviço Realizado</label>
            <textarea name="resultado" rows="4" required></textarea>
        </div>

        <div class="grupo">
            <label>Fotos de Fechamento (opcional)</label>
            <!-- 🔥 CORRETO PARA MULTER -->
            <input type="file" name="fotos" accept="image/*" multiple>
        </div>

        <button class="btn btn-verde-claro btn-full">
            <i class="fa-solid fa-check"></i> Fechar OS
        </button>

    </form>

</div>

<a href="/ordens" class="btn btn-cinza-escuro">
    ← Voltar
</a>

<%- include("./partials/rodape") %>
